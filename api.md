# Web APIs

<!-- TOC -->


# Fetch API


### `fetch()`
`fetch(resource, init)` 

Returns a promise.

`resource` parameter is required.

```javascript
fetchResponsePromise = fetch(resource, init);
```
